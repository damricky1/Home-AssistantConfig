#####
# HACS integration
# https://github.com/bruxy70/Garbage-Collection
#####

garbage_collection:
  sensors:
  - name: "Mixed waste"   # Each week on Fridays
    frequency: "weekly"
    verbose_state: true
    collection_days:
    - fri
  - name: "Recycle"   # Every even weeks on Friday
    frequency: "even-weeks"
    verbose_state: true
    collection_days: "fri"